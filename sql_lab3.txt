DROP TABLE INSTRUCTOR;
CREATE TABLE INSTRUCTOR 
(
    INS_ID INTEGER CONSTRAINT INS_ID PRIMARY KEY NOT NULL,
    LASTNAME VARCHAR(10) NOT NULL,
    FIRSTNAME VARCHAR(10) NOT NULL ,
    CITY VARCHAR(10),
    COUNTRY CHAR(2)
   );
 SELECT *
  FROM INSTRUCTOR;
 INSERT
  INTO INSTRUCTOR
  VALUES (1,'Ahuja','Rav','Toronto','CA');
 INSERT
  INTO INSTRUCTOR
  VALUES (2,'Chong','Raul','Toronto','CA') ,(3,'Vasudevan','Hima','Chicago','US'); 
 SELECT *
  FROM INSTRUCTOR;
 SELECT FIRSTNAME, LASTNAME, COUNTRY
  FROM INSTRUCTOR
  WHERE CITY = 'Toronto';
 UPDATE INSTRUCTOR
  SET LASTNAME ='Markham'
  WHERE INS_ID=1;
 DELETE FROM INSTRUCTOR
  WHERE INS_ID=2;
 SELECT *
  FROM INSTRUCTOR;